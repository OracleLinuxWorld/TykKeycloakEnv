###
### Set up tyk
###

- hosts:         keycloak
  remote_user:   vagrant
  become:        true
  become_user:   root
  become_method: sudo
  gather_facts:  true

  roles:
    - role: role_keycloak
